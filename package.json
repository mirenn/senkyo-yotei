{
  "scripts": {
    "build": "cd frontend && npm run build && cd ../functions && npm run build",
    "deploy": "npm run build && firebase deploy",
    "deploy:hosting": "cd frontend && npm run build && cd .. && firebase deploy --only hosting",
    "deploy:functions": "cd functions && npm run build && cd .. && firebase deploy --only functions",
    "deploy:all": "npm run build && firebase deploy --only hosting,firestore:rules,firestore:indexes"
  },
  "devDependencies": {
    "firebase-admin": "^13.4.0"
  }
}
